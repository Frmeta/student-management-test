Demo
====

A small Gradle-based demo project.

Quick start
-----------
Prerequisites: Java 11+ and the Gradle wrapper (included) & Docker

Start services:
  docker compose up -d

Build (Windows):
  .\gradlew.bat build

Build (macOS/Linux):
  ./gradlew build